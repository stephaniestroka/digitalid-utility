package net.digitalid.utility.generator.generators;

/**
 * Description.
 */
public class BuilderTest {
    
    // Test that the builder is generated correctly (with optional fields being optional, direct build method if no mandatory fields, etc.).
    
}
